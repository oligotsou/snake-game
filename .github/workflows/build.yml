
name: Build Snake EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows-exe:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install pygame pyinstaller

      - name: Build EXE
        run: pyinstaller --onefile --noconsole --name SnakeGame snake_game.py

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: SnakeGame-Windows
          path: dist/SnakeGame.exe
